package org.vivecraft.client.extensions;

public interface TextFeatureRenderExtension {
    /**
     * sets if the late text submits or the regular text submoits should be rendered
     *
     * @param renderLateText if the late text submits should render
     */
    void vivecraft$setRenderLateText(boolean renderLateText);
}
